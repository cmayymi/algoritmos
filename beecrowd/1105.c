#include <stdio.h>

int b, n; // b = numero banco, n = numero debentures

int main()
{
    while (1)
    {
        scanf("%d %d", &b, &n); // quantidade de cartas
        if (b == 0 && n == 0)
            break;
        
    }
}