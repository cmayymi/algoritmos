#include <stdio.h>

int g, p; // g = numero grand prix; p = numero de pilotos

int main()
{
    while (1)
    {
        scanf("%d %d", &g, &p)
        if (g == 0 && p == 0)
            break;
        // segundo input é a ordem de chegada

        // terceiro e o numero de sistema de pontuacao

        //
    }
}